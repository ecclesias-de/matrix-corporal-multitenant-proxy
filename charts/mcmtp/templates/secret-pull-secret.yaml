{{- if .Values.pullSecret.data }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "basename" . }}-pull-secret
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "labels" . | nindent 4 }}
stringData:
  dockerconfigjson: {{ .Values.pullSecret.data  }}
{{- end }}