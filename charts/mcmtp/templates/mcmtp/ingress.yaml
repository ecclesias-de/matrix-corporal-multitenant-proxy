{{- if .Values.mcmtp.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "mcmtp.basename" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "mcmtp.labels" . | nindent 4 }}
  {{- if .Values.mcmtp.ingress.annotations }}
  annotations: {{ .Values.mcmtp.ingress.annotations | toYaml | nindent 4 }}
  {{- end }}
spec:
  rules:
    - host: {{ .Values.mcmtp.ingress.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "mcmtp.basename" . }}
                port:
                  number: 80
  tls:
    - hosts:
        - {{ .Values.mcmtp.ingress.hostname | quote }}
      secretName: {{ printf "%s-tls" .Values.mcmtp.ingress.hostname }}
{{- end }}
